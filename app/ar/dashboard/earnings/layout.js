import { noIndexMetadata } from "../../../seo";

export const metadata = noIndexMetadata("أرباح المدرّب", "/ar/dashboard/earnings", "ar");

export default function ArabicEarningsLayout({ children }) {
  return children;
}
