import { noIndexMetadata } from "../../seo";

export const metadata = noIndexMetadata("الدفع", "/payment/success", "ar");

export default function ArabicPaymentLayout({ children }) {
  return children;
}
