import { noIndexMetadata } from "@/app/seo";

export const metadata = noIndexMetadata("تحليل الاحتياجات", "/needsanalysis", "ar");

export default function ArabicNeedsAnalysisLayout({ children }) {
  return children;
}
