import { noIndexMetadata } from "../../seo";

export const metadata = noIndexMetadata("الملف الشخصي", "/profile", "ar");

export default function ArabicProfileLayout({ children }) {
  return children;
}
