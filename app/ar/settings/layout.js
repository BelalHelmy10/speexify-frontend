import { noIndexMetadata } from "../../seo";

export const metadata = noIndexMetadata("الإعدادات", "/settings", "ar");

export default function ArabicSettingsLayout({ children }) {
  return children;
}
