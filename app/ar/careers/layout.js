import { pageMetadata } from "@/app/seo";
import "@/styles/careers.scss";

export const metadata = pageMetadata("careers", "ar");

export default function ArabicCareersLayout({ children }) {
  return children;
}
