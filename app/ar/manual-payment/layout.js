import { noIndexMetadata } from "../../seo";

export const metadata = noIndexMetadata("الدفع اليدوي", "/manual-payment", "ar");

export default function ArabicManualPaymentLayout({ children }) {
  return children;
}
