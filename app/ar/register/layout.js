import { noIndexMetadata } from "@/app/seo";

export const metadata = noIndexMetadata("إنشاء حساب", "/register", "ar");

export default function ArabicRegisterLayout({ children }) {
  return children;
}
