import { pageMetadata } from "@/app/seo";
import "@/styles/careers.scss";

export const metadata = pageMetadata("careers", "en");

export default function CareersLayout({ children }) {
  return children;
}
